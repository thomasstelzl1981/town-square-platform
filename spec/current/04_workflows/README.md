# 04 — Workflows (Golden Paths)

> Golden Path Definitionen sind in `src/manifests/goldenPaths/` als SSOT definiert.

Dieses Verzeichnis dient als Spec-Referenz. Die kanonische Quelle ist das TypeScript-Manifest.

Siehe: `src/manifests/goldenPaths/index.ts`
